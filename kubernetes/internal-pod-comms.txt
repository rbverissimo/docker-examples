the special address 'localhost' can be used ONLY and ONLY IF containers are inside the same pod and needs to communicate between themselves

kubectl get namespaces 
the command above show kubernetes namespaces. namespeces are a way to group resources. 